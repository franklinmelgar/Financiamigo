<?xml version="1.0" encoding="utf-8"?>
<resources xmlns:tools="http://schemas.android.com/tools">

    <style name="Theme.Financiamigo" parent="android:Theme.Material">
        <item name="colorPrimary">@color/bg_color</item>
    </style>

    <style name="Pantallas" parent="Theme.AppCompat.Light.NoActionBar" />

    <style name="SplashTheme" parent="Theme.AppCompat.NoActionBar">
        <item name="android:windowBackground">@drawable/splash_background</item>
    </style>

    <style name="Theme.MainMenu" parent="Theme.MaterialComponents">

        <!-- color cuando se seleccionar el icono -->
        <item name="colorPrimary">@color/text_color</item>

        <item name="colorPrimaryVariant">@color/text_color</item>

        <!-- color del boton flotante -->
        <item name="colorSecondary">@color/btn_floating</item>

        <!-- color del texto del boton flotante -->
        <item name="colorOnSecondary">@color/text_color</item>

        <item name="android:statusBarColor">@color/bg_color</item>

        <!-- color de la barra -->
        <item name="colorSurface">@color/bg_color</item>


    </style>

    <!-- Base application theme. -->
    <style name="Base.Theme.Financiamigo" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

</resources>